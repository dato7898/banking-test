syntax = "proto3";

package proto;

service PaymentIntegration {
    rpc ProcessPayment(Payment) returns (PaymentResponse);
}

message Payment {
    string id = 1;
    double amount = 2;
    string from = 3;
    string to = 4;
}

message PaymentResponse {
    string status = 1;
}
